# TaskParser - парсер задач с сайта Codeforces, который собирает данные о задачах и их свойствах, сохраняет их в базу данных и обновляет информацию каждый час.


## Стек

- Для работы с базой данных используется фреймворк SQLAlchemy.
- Для хранения данных используется PostgreSQL.
- Используется aiogram для интеграции Telegram в проект.
- Alembic — для управления миграциями баз данных.
- Использован Docker и Docker-compose для контейнеризации проекта.
- Celery и Celery Beat для асинхронной работы некоторых функций проекта, с целью оптимизации.
- Redis для корректной работы Celery и Celery Beat.

## Структура

Проект состоит из:
1. Папка alembic, в которой хранится конфигурация для управления миграциями баз данных.
2. Директория src, в которой находится основной функционал проекта.
3. Папка tests, в которой находятся тесты проекта.
4. .env.sample - пример заполнения env файла.
5. .flake8 - конфигурация для flake8
6. .gitignore - для игнорирования файлов при коммите.
7. docker-compose.yaml - используется для описания всех сервисов, сетей и томов.
8. Dockerfile - для сборки docker-образа из текущего проекта.
9. run_worker.py — исполняемый файл для запуска celery-beat.
10. poetry.lock и poetry.toml - файлы необходимые Poetry для управления зависимостями проекта.


## Использование с помощью docker-compose

1. Клонируйте репозиторий
```
git clone https://github.com/Daniil-Tarasov/TaskParser
```

2. Поскольку проект использует Docker, необходимо установить [Docker](https://www.docker.com/products/docker-desktop/)
3. Откройте скопированный проект.
4. Переименуйте файл '.env.sample' в '.env' и внесите необходимые данные переменных окружения
5. Запустите проект командой: ```docker-compose up -d --build```
6. Для остановки всех контейнеров и их удаления используйте команду ```docker-compose down```


## Проверка работоспособности

1. Общая проверка:
   - Для общей проверки с использованием docker-compose можно воспользоваться командой ```docker-compose logs -f```. Если ошибок не выявлено можно считать проект работоспособным
   - Для проверки каждого из сервисов с использованием docker-compose можно использовать команду ```docker-compose logs <название сериса(например redis)>```
2. Проверка по отдельности:
    - Для проверки сервиса 'telegram-bot' достаточно перейти в ТГ бота в Telegram и убедиться в его отзывчивости
    - Для проверки сервиса 'postgres' можно ввести команду ```docker exec -it <db_container_id> psql -U <username>```. db_container_id можно получить командой ```docker ps```
    - Для проверки сервиса 'redis' введите команду ```docker-compose exec redis redis-cli ping```. Если в терминале получаем ответ 'PONG', значит сервис работает
    - Для проверки сервиса 'celery-worker' введите команду ```docker compose exec celery-worker celery -A src.celery_app inspect ping```. Ожидаемый ответ должен быть 'celery@<hostname>: OK pong'
    - Для проверки сервиса 'celery-beat' достаточно проверить логи командой ```docker-compose logs celery-beat``` и убедиться, что beat scheduler запущен (-beat: Starting // -Scheduler: Sending due task...)


## Тестирование

Все используемые представления в проекте покрыты тестами. Для запуска тестов используйте команду ```pytest```
Для получения отчёта используйте ```pytest --cov=src --cov-report=html```
В момент написания этого файла процент покрытия тестами составляет 86%.